{
  "name": "wingman-server",
  "version": "1.0.0",
  "description": "Wingman API server",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "@vercel/kv": "^0.2.3"
  },
  "devDependencies": {
    "vercel": "^32.5.0"
  }
}
